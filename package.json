{
  "private": true,
  "name": "ascii-server",
  "version": "0.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier -w .",
    "server": "node --env-file-if-exists=config.env src/index.js"
  },
  "packageManager": "pnpm@10.15.0",
  "type": "module",
  "devDependencies": {
    "prettier": "^3.8.1"
  }
}
